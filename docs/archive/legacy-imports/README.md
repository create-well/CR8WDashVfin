# Archived CR8W Legacy Imports

These files were previously stored under `src/imports/**` and `src/lib/mockDashboard.ts`. Repository search found no active application imports or runtime references to them. They are retained here as an archive rather than deleted so the historical Figma snapshots, prompts, screenshots, styles, and mock data remain recoverable.

They are not part of the active Vite application graph and should not be imported into production code without a separate review.
